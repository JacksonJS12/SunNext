@using Microsoft.AspNetCore.Mvc.TagHelpers
@{
    ViewData["Title"] = "Manage Users";
}

<h2 class="mb-4 text-white">👥 Manage Users</h2>

<form method="get" class="row mb-4 g-2">
    <div class="col-md-4">
        <input name="search" class="form-control bg-dark text-white border border-secondary shadow-sm" placeholder="Search by name or email..." />
    </div>
    <div class="col-md-2">
        <button type="submit" class="btn btn-outline-light w-100 shadow-sm">
            <i class="bi bi-search"></i> Search
        </button>
    </div>
</form>

<div class="table-responsive">
    <div class="card bg-glass border-0 shadow-lg">
        <div class="card-body p-0">
            <table class="table table-dark table-hover align-middle mb-0 rounded overflow-hidden">
                <thead class="table-secondary text-dark">
                <tr>
                    <th>Full Name</th>
                    <th>Email</th>
                    <th>Role</th>
                    <th>Systems</th>
                    <th class="text-center">Actions</th>
                </tr>
                </thead>
                <tbody>
                <tr>
                    <td class="fw-semibold">Ivan Petrov</td>
                    <td>ivan@example.com</td>
                    <td><span class="badge bg-primary text-light">User</span></td>
                    <td>3</td>
                    <td class="text-center">
                        <div class="btn-group" role="group">
                            <a href="#" class="btn btn-sm btn-outline-info" title="View">
                                <i class="bi bi-eye"></i>
                            </a>
                            <a href="#" class="btn btn-sm btn-outline-warning" title="Edit">
                                <i class="bi bi-pencil"></i>
                            </a>
                            <a href="#" class="btn btn-sm btn-outline-danger" title="Delete">
                                <i class="bi bi-trash"></i>
                            </a>
                        </div>
                    </td>
                </tr>
                </tbody>
            </table>
        </div>
    </div>
</div>

<style>
    .bg-glass {
        background: rgba(255, 255, 255, 0.05);
        backdrop-filter: blur(10px);
        border-radius: 16px;
    }
</style>